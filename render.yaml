# Render Blueprint — 洁雅股份官网
services:
  - type: web
    name: joyalways
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
